# CoolNote 领域词汇

CoolNote 是本地优先的个人知识与创作资料库。本词汇表统一笔记、小记和画廊等产品概念的称呼，避免把用户内容与底层文件混为一谈。

## Language

**画廊（Gallery）**：
一个具有名称和介绍、用于按人工顺序展示图片的平级内容集合。
_Avoid_: 文件夹、相册目录、作品集类型

**画廊介绍（Gallery Introduction）**：
描述整个画廊主题或用途的一段可换行纯文本。
_Avoid_: 图片说明、富文本正文

**画廊图片（Gallery Item）**：
一张图片在某个画廊中的出现及其展示顺序。
_Avoid_: 图片文件、附件

**图片资产（Gallery Asset）**：
由一个或多个画廊图片共同引用的唯一图片内容。
_Avoid_: 画廊图片、笔记附件

**灯箱（Lightbox）**：
在 CoolNote 应用内完整查看当前画廊图片的沉浸式浏览界面。
_Avoid_: 系统图片查看器、公开展示页

## Relationships

- 一个 **画廊** 包含零个或多个 **画廊图片**
- 一个 **画廊图片** 属于且仅属于一个 **画廊**
- 一个 **画廊图片** 引用且仅引用一个 **图片资产**
- 一个 **图片资产** 可以被多个不同画廊中的 **画廊图片** 引用
- 一个 **画廊** 拥有且仅拥有一份 **画廊介绍**
- 一个 **灯箱** 浏览一个画廊中的当前 **画廊图片** 及其前后相邻项

## Example dialogue

> **Dev:** “把同一张图复制到另一个画廊时，需要再保存一个图片文件吗？”
> **Domain expert:** “不需要。新建的是另一个画廊图片，它继续引用同一个图片资产。”

## Flagged ambiguities

- “画廊在小记下面”被明确为导航位置关系，不表示画廊从属于小记；两者是独立一级入口。
- “资源”在未来可能包含图片、视频和网页；当前画廊范围内只表示图片，不预先建立其他资源类型。
- “作品集展示”被明确为 CoolNote 应用内展示，不表示导出网页、生成公开链接或云端发布。
- “复制灯箱图片”被明确为把图片内容写入系统剪贴板，不表示复制内部文件路径。
- “全局去重”被明确为所有画廊之间去重，不包含笔记附件与画廊资产之间的跨模块去重。
